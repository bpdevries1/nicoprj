# ./scatter2db.tcl -dir ~/Ymor/Philips/KNDL-test -nopost -debug -loglevel debug -moveread
./scatter2db.tcl -dir c:/projecten/Philips/KNDL-test -nopost -debug -loglevel debug -moveread -actions dailystats,gt3,maxitem,aggrsub,analyze

