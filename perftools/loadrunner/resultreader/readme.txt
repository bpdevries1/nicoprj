resultreader - read result1 dir with responses, headers, inf files into sqlite DB.
either separate DB, or the same as the vuser log files.

First copy result1 dir files (or selection) to same vugen dir as where output.txt is copied.

