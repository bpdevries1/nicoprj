Goal
====
* Put mht log file in sqlite database (master-detail) records in order to statistically analyse these client logs.



Notes
=====
* The mht is a mime file, can be exported to html and then converted to xhtml.
* The xhtml contains a namespace, which is a bit ugly to query with xpath, so removed the namespace def.

